{
  buffer.append("<fo:block");
  if (UtilValidate.isNotEmpty(widgetStyle)) {
    buffer.append(" ");
    buffer.append(getFoStyle(widgetStyle));
  }
  buffer.append(">");
  buffer.append(text);
  buffer.append("</fo:block>");
}
