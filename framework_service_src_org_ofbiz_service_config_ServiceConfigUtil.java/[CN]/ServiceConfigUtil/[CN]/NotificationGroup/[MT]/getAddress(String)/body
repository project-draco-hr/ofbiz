{
  List l=FastList.newInstance();
  Iterator i=notify.iterator();
  while (i.hasNext()) {
    Notify n=(Notify)i.next();
    if (n.getType().equals(type)) {
      l.add(n.getValue());
    }
  }
  return l;
}
