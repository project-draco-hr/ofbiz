{
  if (!workEffort.get("workEffortTypeId").equals("ACTIVITY"))   return false;
  try {
    if (getApplications(workEffort).isEmpty())     return false;
  }
 catch (  GenericEntityException ee) {
    Debug.logWarning(ee,module);
    return false;
  }
  return true;
}
