{
  StringBuilder str=new StringBuilder();
  if (index != null) {
    for (int i=0; i < index.length; i++) {
      if (str.length() != 0) {
        str.append(", ");
      }
      str.append(index[i]);
    }
  }
  return str.toString();
}
