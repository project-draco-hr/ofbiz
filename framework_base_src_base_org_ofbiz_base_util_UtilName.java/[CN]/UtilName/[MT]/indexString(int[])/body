{
  StringBuilder str=new StringBuilder();
  if (index != null) {
    for (    int i : index) {
      if (str.length() != 0) {
        str.append(", ");
      }
      str.append(i);
    }
  }
  return str.toString();
}
