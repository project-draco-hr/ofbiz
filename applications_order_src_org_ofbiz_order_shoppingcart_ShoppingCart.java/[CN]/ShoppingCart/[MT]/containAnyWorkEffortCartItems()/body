{
  for (int i=0; i < this.cartLines.size(); i++) {
    ShoppingCartItem cartItem=cartLines.get(i);
    if (cartItem.getItemType().equals("RENTAL_ORDER_ITEM")) {
      return true;
    }
  }
  return false;
}
