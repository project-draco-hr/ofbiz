{
  if (idx == -1) {
    return null;
  }
  if (shipInfo.size() == 0) {
    CartShipInfo csi=new CartShipInfo();
    csi.orderTypeId=getOrderType();
    shipInfo.add(csi);
  }
  return (CartShipInfo)shipInfo.get(idx);
}
