{
  boolean taxApplies=false;
  Iterator<ShoppingCartItem> i=this.iterator();
  while (i.hasNext()) {
    ShoppingCartItem item=i.next();
    if (item.taxApplies()) {
      taxApplies=true;
      break;
    }
  }
  return taxApplies;
}
