{
  boolean taxApplies=false;
  Iterator i=this.iterator();
  while (i.hasNext()) {
    ShoppingCartItem item=(ShoppingCartItem)i.next();
    if (item.taxApplies()) {
      taxApplies=true;
      break;
    }
  }
  return taxApplies;
}
