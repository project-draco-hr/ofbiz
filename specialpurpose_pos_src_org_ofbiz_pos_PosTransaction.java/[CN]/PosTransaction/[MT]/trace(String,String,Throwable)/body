{
  if (trace != null) {
    String msg=s1;
    if (UtilValidate.isNotEmpty(s2)) {
      msg=msg + "(" + s2+ ")";
    }
    if (t != null) {
      msg=msg + " : " + t.getMessage();
    }
    trace.println("[POS @ " + terminalId + " TX:"+ transactionId+ "] - "+ msg);
    trace.flush();
  }
}
