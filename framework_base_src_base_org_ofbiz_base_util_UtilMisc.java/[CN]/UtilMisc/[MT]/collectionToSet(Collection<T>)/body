{
  if (c == null)   return null;
  Set theSet=null;
  if (c instanceof Set) {
    theSet=(Set)c;
  }
 else {
    theSet=FastSet.newInstance();
    c.remove(null);
    theSet.addAll(c);
  }
  return theSet;
}
