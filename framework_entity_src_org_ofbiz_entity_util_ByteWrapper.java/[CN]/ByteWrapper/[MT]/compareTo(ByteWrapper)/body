{
  int r=bytes.length - other.bytes.length;
  if (r != 0)   return r;
  int i=0;
  for (i=0; i < bytes.length; i++) {
    r=bytes[i] - other.bytes[i];
    if (r != 0)     return r;
  }
  return 0;
}
