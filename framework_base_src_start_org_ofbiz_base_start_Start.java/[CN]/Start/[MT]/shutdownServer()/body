{
  if (serverStopping)   return;
  serverStopping=true;
  if (loaders != null && loaders.size() > 0) {
    for (    StartupLoader loader : loaders) {
      try {
        loader.unload();
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
  serverRunning=false;
}
