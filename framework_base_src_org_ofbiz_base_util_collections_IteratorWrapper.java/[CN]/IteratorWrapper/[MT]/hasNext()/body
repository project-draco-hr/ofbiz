{
  if (nextCalled)   return true;
  if (!it.hasNext())   return false;
  do {
    lastSrc=it.next();
    if (isValid(lastSrc)) {
      nextCalled=true;
      lastDest=convert(lastSrc);
      return true;
    }
  }
 while (it.hasNext());
  return false;
}
