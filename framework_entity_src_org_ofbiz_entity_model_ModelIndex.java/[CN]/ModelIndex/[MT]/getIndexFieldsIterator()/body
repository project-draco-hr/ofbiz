{
  return new IteratorWrapper<String,Field>(this.fields.iterator()){
    @Override protected void noteRemoval(    String dest,    Field src){
    }
    @Override protected String convert(    Field src){
      return src.getFieldName();
    }
  }
;
}
