{
  if (fileTable != null) {
    try {
      return fileTable.keys().next() == null;
    }
 catch (    IOException e) {
      Debug.logError(e,module);
      return false;
    }
  }
 else {
    if (isNullSet) {
      return false;
    }
 else {
      return memoryTable.isEmpty();
    }
  }
}
