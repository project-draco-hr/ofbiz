{
  if (this.version.equals("3.0")) {
    if (posNum == this.CID_RESPONSE_CODE)     return "M";
  }
  if (posNum < 1 || posNum > maxPos) {
    return "unknown_field";
  }
 else {
    return (String)response.get(posNum);
  }
}
