{
  if (userLogin == null && this.userLoginId != null) {
    Delegator delegator=this.getDelegator();
    if (delegator != null) {
      this.userLogin=delegator.findOne("UserLogin",UtilMisc.toMap("userLoginId",this.userLoginId),false);
    }
  }
  return this.userLogin;
}
