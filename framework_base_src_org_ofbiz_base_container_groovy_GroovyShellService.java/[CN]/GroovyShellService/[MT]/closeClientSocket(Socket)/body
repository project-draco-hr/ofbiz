{
  try {
    if (socket != null) {
      log.info("Closing client socket: " + socket);
      socket.close();
    }
  }
 catch (  IOException e) {
    log.error("Could not close client socket: " + socket,e);
  }
}
