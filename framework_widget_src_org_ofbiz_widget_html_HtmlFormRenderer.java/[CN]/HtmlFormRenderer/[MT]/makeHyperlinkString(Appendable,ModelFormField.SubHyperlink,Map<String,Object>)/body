{
  if (subHyperlink == null) {
    return;
  }
  if (subHyperlink.shouldUse(context)) {
    writer.append(' ');
    this.makeHyperlinkString(writer,subHyperlink.getLinkStyle(),subHyperlink.getTargetType(),subHyperlink.getTarget(context),subHyperlink.getDescription(context),subHyperlink.getTargetWindow(context),null,null);
  }
}
