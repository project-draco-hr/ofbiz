{
  Iterator<String[]> i=functionStack.iterator();
  while (i.hasNext()) {
    String[] func=i.next();
    if (func[0].equals(function)) {
      i.remove();
      return func;
    }
  }
  return null;
}
