{
  if (field == null)   return;
synchronized (fieldsLock) {
    fieldsMap.put(field.getName(),field);
    if (field.getIsPk()) {
      pks.add(field);
    }
 else {
      nopks.add(field);
    }
  }
}
