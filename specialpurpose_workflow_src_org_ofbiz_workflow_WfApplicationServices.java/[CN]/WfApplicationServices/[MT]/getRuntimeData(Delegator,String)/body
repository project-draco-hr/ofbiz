{
  try {
    GenericValue application=delegator.findOne("ApplicationSandbox",UtilMisc.toMap("applicationId",applicationId),false);
    return application.getRelatedOne("RuntimeData");
  }
 catch (  GenericEntityException ee) {
    throw new GenericServiceException(ee.getMessage(),ee);
  }
}
