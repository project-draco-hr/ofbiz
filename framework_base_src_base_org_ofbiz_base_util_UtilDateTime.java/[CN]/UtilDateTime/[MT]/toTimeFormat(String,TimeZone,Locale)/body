{
  DateFormat df=null;
  if (timeFormat == null) {
    df=DateFormat.getDateInstance(DateFormat.MEDIUM,locale);
  }
 else {
    df=new SimpleDateFormat(timeFormat);
  }
  df.setTimeZone(tz);
  return df;
}
