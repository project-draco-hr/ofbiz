{
  int currentIdx=0;
  if (isNullSet) {
    if (currentIdx == index) {
      return null;
    }
    currentIdx++;
  }
  for (  K key : memoryTable.keySet()) {
    if (currentIdx == index) {
      return key;
    }
    currentIdx++;
  }
  return null;
}
