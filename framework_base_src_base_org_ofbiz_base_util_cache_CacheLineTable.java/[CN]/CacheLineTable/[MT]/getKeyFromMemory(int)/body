{
  Iterator i=memoryTable.keySet().iterator();
  int currentIdx=0;
  if (isNullSet) {
    if (currentIdx == index) {
      return null;
    }
    currentIdx++;
  }
  while (i.hasNext()) {
    Object key=i.next();
    if (currentIdx == index) {
      return key;
    }
    currentIdx++;
  }
  return null;
}
