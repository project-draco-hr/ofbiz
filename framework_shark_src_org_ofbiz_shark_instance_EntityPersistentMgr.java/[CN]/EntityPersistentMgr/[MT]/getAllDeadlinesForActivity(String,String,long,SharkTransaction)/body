{
  List lookupList=getDeadlineValues(UtilMisc.toList(new EntityExpr("processId",EntityOperator.EQUALS,procId),new EntityExpr("activityId",EntityOperator.EQUALS,actId),new EntityExpr("timeLimit",EntityOperator.LESS_THAN,new Long(timeLimit))));
  return getDealineObjects(lookupList);
}
