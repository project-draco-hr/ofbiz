{
  writer.write('[');
  writer.push();
  if (it.hasNext())   writer.newline();
  while (it.hasNext()) {
    write(it.next());
    if (it.hasNext())     writer.write(',');
    writer.newline();
  }
  writer.pop();
  writer.write(']');
  return this;
}
