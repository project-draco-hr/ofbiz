{
  writer.write('[');
  writer.push();
  for (int i=0; i < o.length; i++) {
    if (i != 0)     writer.write(',');
    writer.newline();
    write(o[i]);
  }
  if (o.length > 0)   writer.newline();
  writer.pop();
  writer.write(']');
  return this;
}
