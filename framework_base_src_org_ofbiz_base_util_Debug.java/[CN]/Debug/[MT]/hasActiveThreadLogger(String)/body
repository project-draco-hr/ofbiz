{
  Logger threadGroupLogger=org.apache.log4j.LogManager.exists(threadGroupId);
  return threadGroupLogger != null && (activeThreadGroupLoggerMap.get(threadGroupId) != null && activeThreadGroupLoggerMap.get(threadGroupId).size() > 0);
}
