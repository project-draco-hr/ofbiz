{
  Map uri=getRequestMapMap(uriStr);
  if (uri != null && attribute != null) {
    return (String)uri.get(attribute);
  }
 else {
    Debug.logInfo("[RequestManager.getRequestAttribute] Value for attribute \"" + attribute + "\" of uri \""+ uriStr+ "\" not found",module);
    return null;
  }
}
