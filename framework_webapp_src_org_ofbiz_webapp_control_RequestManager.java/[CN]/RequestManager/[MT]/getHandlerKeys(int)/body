{
  Map map=getHandlerMap();
  Map hMap;
  if (type == 1) {
    hMap=(Map)map.get("view");
  }
 else {
    hMap=(Map)map.get("event");
  }
  if (hMap != null) {
    return new LinkedList(hMap.keySet());
  }
 else {
    return null;
  }
}
