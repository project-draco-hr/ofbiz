{
  List queuedList=new ArrayList(eventQueue);
  this.eventQueue=new ArrayList();
  Iterator i=queuedList.iterator();
  while (i.hasNext()) {
    Object obj=i.next();
    i.remove();
    this.fireEvent(obj);
  }
}
