{
  this.outputLocation=args[0];
  for (int i=1; i < args.length; i++) {
    if ("-ant".equals(args[i])) {
      mode="ant";
    }
 else     if ("-text".equals(args[i])) {
      mode="text";
    }
  }
}
