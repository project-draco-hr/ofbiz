{
  if (functionName != null)   sb.append(functionName).append('(');
  sb.append(fieldName);
  if (functionName != null)   sb.append(')');
switch (order) {
case ASCENDING:
    sb.append(" ASC");
  break;
case DESCENDING:
sb.append(" DESC");
break;
}
return sb;
}
